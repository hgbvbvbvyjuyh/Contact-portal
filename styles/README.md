# Styles Directory

The global styles for this application are configured in `/src/index.css` using Tailwind CSS v4.

This directory is reserved for global CSS overrides, theme files, or utility stylesheets if needed in the future.
